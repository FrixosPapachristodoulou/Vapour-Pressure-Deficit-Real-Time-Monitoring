# --- core UI / data viz ----------------------------------------------------
streamlit==1.45.0
altair==5.5.0
pydeck==0.9.1
matplotlib==3.9.2         # first 3.9.x build with cp313 wheels

# --- data & scientific stack ----------------------------------------------
numpy==2.3.0
pandas==2.3.0
pyarrow==20.0.0            # feather & parquet support
meteostat==1.6.3           # weather data for VPD calculations

# --- plotting dependencies -------------------------------------------------
contourpy==1.2.1
cycler==0.12.1
fonttools==4.53.1
kiwisolver==1.4.5
pillow==10.4.0

# --- Streamlit internals & web stack ---------------------------------------
blinker==1.9.0
click==8.1.8
tornado==6.4.2
watchdog==6.0.0
protobuf==6.30.2

# --- general utilities -----------------------------------------------------
attrs==25.3.0
cachetools==5.5.2
certifi==2024.7.4
charset-normalizer==3.3.2
idna==3.7
requests==2.32.3
urllib3==2.2.2
python-dateutil==2.9.0.post0
pytz==2025.2
tzdata==2025.2
typing_extensions==4.13.2
packaging==24.1
pyparsing==3.1.2
six==1.16.0
tenacity==9.1.2

# --- JSON & schema handling ------------------------------------------------
jsonschema==4.23.0
jsonschema-specifications==2025.4.1
referencing==0.36.2
rpds-py==0.24.0

# --- Git integration -------------------------------------------------------
GitPython==3.1.44
gitdb==4.0.12
smmap==5.0.2

# --- misc (remove if unused) ----------------------------------------------
narwhals==1.37.1           # optional but pinned for reproducibility
toml==0.10.2               # only if you parse legacy pyproject.toml

# --------------------------------------------------------------------------
# If your app needs system libraries (e.g. for OpenCV),
# add a `packages.txt` in the repo root with the required apt packages.
#
# Remember to commit **runtime.txt** with:
#     python-3.11
# or python-3.12 if you prefer.
